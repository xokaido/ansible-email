driver = mysql
connect = "host={{ mail_db_hostname }} dbname={{ mail_db_database }} user={{ mail_db_username }} password={{ mail_db_password }}"
default_pass_scheme = SHA512-CRYPT
password_query = SELECT email as User, password FROM users WHERE email='%u';
user_query = SELECT email FROM users WHERE email = '%u'

